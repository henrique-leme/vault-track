version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb_container
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: vault-tracker

volumes:
  mongo-data:
